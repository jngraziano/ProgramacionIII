<?php
MySQL
Pagina de ingreso: http://localhost:8080/phpmyadmin/
Creamos la base de datos --> *Nombre* tipo: utf8_spanish2_ci (es para que este en español)
Creamos la tabla con 4 filas
la primera le colocamos primary (para que sea la key) de nombre ID y autoincremental.
y completamos las demas columnas.
Vamos a la solapa SQL y hacemos click en el INSERT para agregar dato (los comandos ya se ponen todo)
sacamos la value 1 ya que es la key (no debemos modificiar) Este ID es unico e irrepetible por ende 
si borramos un producto el numero de ID no se vuelve a repetir
Creamos un archivo para recordar las lineas que hicimos llamado InstruccionesMySql.txt

Instruccion where: son las condiciones que se definen para limitar la ejecucion de la consulta o expandir la ejecucion de la consulta
RECORDAR SIEMPRE DE HACER LA PRIMARY KEY INCREMENTAL






?>